# Блок 28. Async SQLAlchemy, нагрузка и наблюдаемость

**Занятия:** 159–164.

**Назначение:** Перевести database I/O на AsyncSession и измерить результат.

## 159. AsyncEngine и async driver PostgreSQL

### Цель

Создать асинхронную конфигурацию базы и понять роль driver.

### Основное содержание

- sync vs async driver;
- async PostgreSQL URL;
- `create_async_engine`;
- `AsyncEngine`;
- pool;
- проверка соединения;
- отдельная config;
- поэтапная migration;

### Практика StudyHub

Подключить async driver, выполнить `SELECT 1`, не удаляя sync-версию.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

AsyncEngine соединяется с существующей PostgreSQL-базой.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
