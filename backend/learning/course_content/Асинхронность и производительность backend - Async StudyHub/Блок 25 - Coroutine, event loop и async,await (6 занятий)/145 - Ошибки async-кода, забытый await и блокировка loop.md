## 145. Ошибки async-кода: забытый await и блокировка loop

### Цель

Диагностировать coroutine object вместо результата и blocking call внутри async-кода.

### Основное содержание

- forgotten await;
- never awaited warning;
- coroutine вместо data;
- `time.sleep` внутри async def;
- blocked event loop;
- `asyncio.sleep`;
- blocking library;
- диагностический порядок;

### Практика StudyHub

Исправить четыре сломанных async-примера.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик находит забытый await и blocking call.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
