# Блок 27. Асинхронный FastAPI и внешние HTTP-сервисы

**Занятия:** 153–158.

**Назначение:** Применить async к реальному сетевому I/O и тестировать интеграцию без сети.

## 153. def и async def внутри FastAPI

### Цель

Выбирать форму endpoint по используемым библиотекам и характеру работы.

### Основное содержание

- sync endpoint;
- async endpoint;
- threadpool для `def` на прикладном уровне;
- blocking call внутри async;
- async-compatible library;
- CPU-bound endpoint;
- смешанное приложение;
- decision rule;

### Практика StudyHub

Классифицировать endpoints StudyHub и переписать только реальный I/O-сценарий.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик аргументирует выбор `def` или `async def`.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
