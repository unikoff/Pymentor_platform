## 150. Cancellation и обязательная очистка

### Цель

Понять отмену как отдельный сценарий и гарантировать cleanup.

### Основное содержание

- `task.cancel()`;
- `CancelledError`;
- момент cancellation;
- `finally`;
- cleanup;
- повторное пробрасывание;
- не скрывать широким except;
- cancelled state;

### Практика StudyHub

Отменить долгую операцию и подтвердить выполнение finally.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик корректно обрабатывает отмену.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
