## 149. Timeout через asyncio.wait_for

### Цель

Ограничить максимальное время внешней операции.

### Основное содержание

- `asyncio.wait_for`;
- `TimeoutError`;
- локальный timeout;
- общий timeout;
- разумное значение;
- timeout не retry;
- fallback;
- лог причины;

### Практика StudyHub

Добавить timeout к медленной загрузке рекомендаций.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Операция имеет предсказуемое ограничение ожидания.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
