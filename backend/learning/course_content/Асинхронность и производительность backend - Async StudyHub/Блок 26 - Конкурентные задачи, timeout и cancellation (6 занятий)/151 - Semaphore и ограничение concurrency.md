## 151. Semaphore и ограничение concurrency

### Цель

Не запускать неограниченное количество внешних операций.

### Основное содержание

- `asyncio.Semaphore`;
- максимум active tasks;
- `async with`;
- очередь ожидания;
- throughput;
- слишком большой limit;
- active count;
- config limit;

### Практика StudyHub

Обработать 20 учебных запросов максимум по 3 одновременно.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Агрегатор соблюдает установленный предел concurrency.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;

<!-- youtube: -->
