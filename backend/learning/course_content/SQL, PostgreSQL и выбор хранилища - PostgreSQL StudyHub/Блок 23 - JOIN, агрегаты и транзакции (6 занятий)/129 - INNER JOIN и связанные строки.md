# Блок 23. JOIN, агрегаты и транзакции

**Занятия:** 129–134.

**Назначение:** Перейти от простого CRUD к связанным данным, статистике и атомарным операциям.

## 129. INNER JOIN и связанные строки

### Цель

Соединить задачи с пользователями и категориями по foreign key.

### Основное содержание

- foreign key;
- `INNER JOIN`;
- `ON`;
- qualified columns;
- aliases;
- дублирование родителя в result;
- join в SQLAlchemy;
- ошибка условия соединения;

### Практика StudyHub

Получить задачи с username и category name, затем повторить SQLAlchemy statement.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик объясняет происхождение каждой колонки результата.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;

<!-- youtube: -->
