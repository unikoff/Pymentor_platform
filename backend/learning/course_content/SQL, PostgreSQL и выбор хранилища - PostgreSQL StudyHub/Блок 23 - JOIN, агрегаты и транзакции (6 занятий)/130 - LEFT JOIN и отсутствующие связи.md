## 130. LEFT JOIN и отсутствующие связи

### Цель

Сохранить строки левой таблицы, даже когда связанного объекта нет.

### Основное содержание

- `LEFT JOIN`;
- NULL справа;
- nullable foreign key;
- INNER vs LEFT;
- условие в `ON` и `WHERE`;
- родители без детей;
- outerjoin;
- ошибка превращения LEFT в INNER;

### Практика StudyHub

Получить все задачи, включая задачи без категории, и пользователей без задач.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик выбирает тип JOIN по требуемому набору.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
