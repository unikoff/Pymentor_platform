# Блок 22. PostgreSQL и перенос StudyHub

**Занятия:** 123–128.

**Назначение:** Перенести рабочий API с SQLite на PostgreSQL без изменения HTTP-контракта.

## 123. PostgreSQL: сервер, база, схема и подключение

### Цель

Разделить понятия, которые в SQLite были скрыты одним файлом.

### Основное содержание

- server process;
- database;
- schema `public`;
- role/user;
- host и port;
- client connection;
- connection URL;
- PostgreSQL против SQLite;

### Практика StudyHub

Нарисовать маршрут FastAPI → driver → server → database → schema → table.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик не путает сервер, базу, схему и роль.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;
