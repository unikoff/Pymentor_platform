## 120. SELECT, WHERE, ORDER BY, LIMIT и OFFSET

### Цель

Собрать читаемый запрос выборки по последовательным этапам.

### Основное содержание

- `SELECT` и `FROM`;
- `WHERE`;
- `AND`/`OR`;
- `IS NULL`;
- `ORDER BY`;
- `LIMIT`;
- `OFFSET`;
- логический порядок обработки запроса;

### Практика StudyHub

Получить открытые задачи нужного приоритета, отсортировать и взять страницу; повторить через SQLAlchemy.

### Визуальная подача

Использовать схему движения данных, карточки ролей, сравнение корректного и ошибочного подхода, пошаговый разбор и одну доминирующую интерактивную механику.

### Результат занятия

Ученик пишет фильтрованный и пагинированный SELECT.

### Критерии готовности

- ученик объясняет главную модель своими словами;
- запускает минимальный пример;
- изменяет один параметр и предсказывает эффект;
- проверяет успешный и ошибочный сценарий;
- фиксирует результат отдельным Git-коммитом;

<!-- youtube: -->
