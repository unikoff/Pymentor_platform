# План обучения: этап 4

## Паспорт страницы

- Этап: 4.
- Проект: StudyHub Database API.
- Экспорт frontend: `LearningRoadmap`.
- Назначение: вводная и справочная страница этапа.

## Введение

За 24 занятия StudyHub Planner API превратится в Database API: сначала приложение получит понятный request pipeline и зависимости, затем SQLite и SQLAlchemy, полноценные запросы, связи и воспроизводимую схему через Alembic.

## 1. Откуда начинается этап 4

Перед началом этапа уже существует Planner API: endpoints, Pydantic-схемы, CRUD в памяти, routers, TestClient, Swagger и Postman. Следующий шаг не переписывает проект с нуля — он заменяет временные границы на устойчивые.

1. **Сохранить знакомый HTTP-контракт.** Methods, paths, schemas, statuses и tests остаются точкой опоры.
2. **Увидеть внутренний путь request.** До базы нужно понять Depends, settings, headers, middleware и CORS.
3. **Заменить память SQLite.** Список внутри процесса уступает таблицам, engine и session.
4. **Развить запросы.** CRUD переходит к select, where, sort, pagination, count и transactions.
5. **Зафиксировать схему.** Relations и Alembic делают структуру данных воспроизводимой.

Итогом станет StudyHub Database API, который переживает перезапуск и может объяснимо развивать схему данных.

### Эволюция проекта

```text
Planner API
  ↓ request pipeline и Depends
FastAPI как цельное приложение
  ↓ SQLite + SQLAlchemy
Database-backed API
  ↓ CRUD queries + transactions
Reliable Database API
  ↓ relations + Alembic
StudyHub Database API
```

### Сравнение: Какая часть должна измениться первой?

**Вариант A — Переписать весь API**

```text
new_project = True
old_routes = delete_all()
```

Резкий старт уничтожает знакомый контекст и смешивает много новых проблем.

**Вариант B — Сохранить контракт**

```text
same_routes = True
storage = SQLiteStorage()
```

Клиентский договор остаётся знакомым, а способ хранения меняется постепенно.

**Разбор:** Плавный маршрут сохраняет endpoints и Pydantic-схемы, меняя внутренний механизм небольшими шагами.

> Этап 4 не начинается с SQL-запроса внутри случайного endpoint. Сначала приложение получает ясные зависимости и конфигурацию, чтобы база подключилась в подготовленную структуру.

> Сквозной проект сохраняет знакомый домен задач, чтобы внимание уходило на database-механизмы, а не на новую предметную область.

### HTTP-контракт

**Метка:** `сохранить`.

Methods, paths, Pydantic schemas и statuses остаются знакомыми.

```text
POST /tasks → 201
```

### Временное storage

**Метка:** `заменить`.

Python list постепенно заменяется Session и database rows.

```text
list → SQLite
```

### Schema history

**Метка:** `добавить`.

Alembic фиксирует изменения tables и columns во времени.

```text
revision → upgrade
```

### Что уже умеет ученик

Запустить FastAPI, прочитать request, проверить response и найти endpoint.

### Что станет новым

Проследить connection, Session, statement, transaction и migration.

### Что останется знакомым

Функции, type hints, exceptions, pytest и декомпозиция продолжают работать.

## 2. Четыре блока одного проекта

Блоки 13–16 не являются независимыми мини-курсами. Каждый следующий использует результат предыдущего и отвечает на уже заметное ограничение StudyHub.

| Понятие | Смысл |
| --- | --- |
| `Блок 13 · FastAPI как цельное приложение` | Request pipeline, Depends, Annotated, Settings, headers, cookies, middleware и CORS. |
| `Блок 14 · SQLite и основы SQLAlchemy` | Причина появления базы, engine, URL, Declarative Base, ORM-модель, таблицы, Session и get_db. |
| `Блок 15 · CRUD и запросы SQLAlchemy` | SELECT, изменение данных, WHERE, sorting, pagination, COUNT, EXISTS, uniqueness и transactions. |
| `Блок 16 · Связи, Alembic и Database API` | Foreign key, one-to-many, relationship, loading, N+1, migrations и итоговая защита. |

### Соберите маршрут этапа

Расположите крупные результаты в порядке появления.

Правильный порядок:
1. `увидеть полный request pipeline`
2. `вынести повторяемую подготовку в dependencies`
3. `создать engine и ORM model`
4. `провести первую session transaction`
5. `реализовать CRUD и динамические queries`
6. `добавить категории и one-to-many`
7. `зафиксировать schema через Alembic`
8. `защитить StudyHub Database API`

Каждый слой появляется после того, как понятна проблема, которую он решает.

> Нельзя перескочить из in-memory списка сразу в сложный repository/service/unit-of-work набор. На этом этапе важнее увидеть engine, session, transaction и query без лишней архитектурной маскировки.

### После блока 13

Ученик показывает порядок middleware, dependencies и endpoint на одном request.

### После блока 14

Ученик открывает SQLite viewer и связывает ORM attribute с реальной column.

### После блока 15

Ученик читает statement и объясняет transaction success/error path.

### После блока 16

Ученик поднимает пустую database только через migrations и запускает tests.

### Контрольные результаты блоков

```text
после блока 13 → request pipeline объясняется
после блока 14 → первая row сохраняется в SQLite
после блока 15 → CRUD работает через SQLAlchemy queries
после блока 16 → schema воспроизводится migrations
```

### Подготовить приложение

**Метка:** `13`.

Dependencies, Settings и middleware создают ясные инфраструктурные границы.

### Подключить и использовать базу

**Метка:** `14–15`.

Engine, models, Session и statements заменяют временный список.

### Связать и выпускать

**Метка:** `16`.

Relations и Alembic завершают Database API.

## 3. Блок 13: подготовка FastAPI к базе

Перед SQLAlchemy приложение должно ясно отвечать на вопросы: как request доходит до endpoint, где живёт конфигурация, как передаётся зависимость и где размещается общее HTTP-поведение.

| Понятие | Смысл |
| --- | --- |
| `69 · Request pipeline` | Uvicorn → routing → parsing → validation → dependencies → endpoint → response. |
| `70 · Первый Depends` | Повторяемая подготовка становится отдельной функцией с понятным контрактом. |
| `71 · Annotated и цепочки` | Aliases и короткие sub-dependencies делают сигнатуру читаемой. |
| `72 · Settings` | Environment и .env отделяют конфигурацию от исходного кода. |
| `73 · Headers, cookies, Response` | HTTP-контракт расширяется за пределы path, query и body. |
| `74 · Middleware и CORS` | Общее поведение охватывает каждый request, а frontend получает точное разрешение origin. |

### Путь, который станет основой get_db

```text
client
→ Uvicorn
→ middleware before
→ route matching
→ input validation
→ dependencies
→ endpoint
→ response serialization
→ middleware after
→ client
```

### Разбор сценариев

```text
request пришёл
route найден
input valid
dependencies solved
endpoint called
response returned
```

- `неверный path type` → строка 3 → `422 before endpoint`
- `dependency raises 404` → строка 4 → `404 before endpoint body`
- `обычный request` → строка 6 → `200/201 response`

> Главная подготовка к блоку 14 — понять, что get_db будет не магической командой, а dependency с управляемым жизненным циклом ресурса.

### Сначала наблюдать

Отправить request и найти его в middleware, router и endpoint.

### Затем вынести

Повторяемое получение configuration или resource превратить в dependency.

### После этого связать

Создать короткую dependency chain и проверить порядок выполнения.

### Только затем расширить

Добавить headers, cookies, response metadata и CORS.

### ручная проверка блока 13

```text
$ python -m uvicorn app.main:app --reload
INFO: Application startup complete
$ curl -i http://127.0.0.1:8000/health
HTTP/1.1 200 OK
x-request-id: ...
```

## 4. Блок 14: SQLite и первые ORM-объекты

После блока 13 временное состояние процесса заменяется файлом SQLite. SQLAlchemy вводится от подключения и модели к session, а не с готового многослойного шаблона.

### Файл базы

**Метка:** `SQLite`.

Хранит таблицы и строки после завершения процесса.

```text
studyhub.db
```

### Точка подключения

**Метка:** `engine`.

Знает database URL и создаёт соединения по необходимости.

```text
create_engine(...)
```

### Модель таблицы

**Метка:** `ORM`.

Связывает Python-класс, columns, keys и SQL-таблицу.

```text
class Task(Base): ...
```

| Понятие | Смысл |
| --- | --- |
| `75 · Зачем база` | Потеря данных, несколько процессов, уникальность и запросы формулируют требования. |
| `76 · Engine и URL` | sqlite:///./studyhub.db и connect_args становятся понятной конфигурацией. |
| `77 · Declarative Base` | Task получает __tablename__, mapped_column, primary key и types. |
| `78 · Создание таблиц` | Metadata создаёт schema, а SQLite viewer подтверждает фактический результат. |
| `79 · Session lifecycle` | add → commit → refresh; при ошибке rollback; в конце close. |
| `80 · get_db` | FastAPI dependency выдаёт Session endpoint и гарантирует освобождение ресурса. |

### Сравнение: Что честнее описывает данные после перезапуска?

**Вариант A — In-memory list**

```text
tasks = []
# process stops → data gone
```

Подходит для учебного HTTP-контракта, но не обеспечивает устойчивое хранение.

**Вариант B — SQLite table**

```text
tasks table
# process stops → rows remain
```

Файл базы сохраняет состояние и поддерживает запросы.

**Разбор:** База появляется как решение наблюдаемой проблемы, а не как обязательный атрибут любого маленького API.

> SQLite используется как самый спокойный первый шаг: один локальный файл позволяет изучить SQLAlchemy без отдельного сервера PostgreSQL.

### проверка первого database milestone

```text
$ python -m app.create_tables
tables created
$ sqlite3 studyhub.db ".schema tasks"
CREATE TABLE tasks (...)
$ python -m pytest -q tests/test_database.py
4 passed
```

**Критерий готовности блока 14: ученик может показать одну row одновременно как JSON response, ORM object и запись SQLite.**

1. **Создать database URL.** Settings предоставляет строку подключения без жёсткой привязки к коду.
2. **Создать engine.** Engine знает dialect, driver и место database file.
3. **Объявить Base и models.** Metadata собирает описание tables и columns.
4. **Создать schema.** Tables появляются фактически и проверяются viewer или SQL query.
5. **Открыть Session.** Первая transaction создаёт row и возвращает generated id.
6. **Подключить get_db.** FastAPI получает Session на время одного request.

Этот маршрут выполняется последовательно; попытка начать с get_db без engine и SessionLocal оставляет dependency без ресурса.

### Первая вертикальная цепочка

```text
Settings.database_url
→ create_engine
→ SessionLocal
→ Base.metadata
→ Task ORM model
→ get_db
→ POST /tasks
→ INSERT + COMMIT
→ TaskRead response
```

## 5. Блок 15: CRUD, фильтры и транзакции

Когда Session уже понятна, familiar CRUD переводится с операций списка на SQL expressions. Сначала простые SELECT и изменения, затем composable filters и только после этого ограничения и ошибки транзакции.

| Понятие | Смысл |
| --- | --- |
| `81 · SELECT` | Список через select(Task) и item через session.get или WHERE. |
| `82 · Create, update, delete` | Изменить ORM-объект, commit и refresh; удалить и подтвердить transaction. |
| `83 · WHERE` | Условия добавляются к statement только для переданных query-параметров. |
| `84 · Sorting и pagination` | order_by, limit и offset формируют предсказуемую страницу. |
| `85 · COUNT, EXISTS, uniqueness` | База отвечает на агрегатные вопросы и защищает уникальные значения. |
| `86 · Transaction и IntegrityError` | Ошибка commit требует rollback перед следующим использованием Session. |

### Эволюция запроса

```text
statement = select(Task)

if is_done is not None:
    statement = statement.where(Task.is_done == is_done)

if min_priority is not None:
    statement = statement.where(Task.priority >= min_priority)

statement = statement.order_by(Task.id).offset(offset).limit(limit)

items = session.scalars(statement).all()
```

### Сопоставление: Соедините SQLAlchemy-операцию и смысл.

- `select(Task)` → построить запрос к задачам
- `.where(...)` → добавить условие
- `.order_by(...)` → задать порядок
- `.limit(...)` → ограничить размер страницы
- `session.commit()` → подтвердить transaction
- `session.rollback()` → вернуть Session в рабочее состояние после ошибки

Statement описывает SQL, Session выполняет его и управляет transaction.

> Главный навык блока — не запомнить цепочку методов, а уметь прочитать statement слева направо и объяснить, какой SQL-смысл добавляет каждый шаг.

1. **List query.** Сначала получить предсказуемый список с ORDER BY.
2. **Item query.** Затем вернуть row по id или 404.
3. **Write operations.** После чтения добавить create, update и delete.
4. **Dynamic filters.** Только затем собирать statement по optional query parameters.
5. **Aggregates.** COUNT и EXISTS отвечают на отдельные database-вопросы.
6. **Integrity path.** Завершить блок конфликтом constraint и обязательным rollback.

Порядок сохраняет низкий порог входа: каждый новый query использует уже знакомую Session и statement.

### Прочитать statement

Назвать SELECT target, WHERE conditions, ORDER BY и page boundaries.

### Предсказать rows

До запуска определить, какие задачи должны пройти filters.

### Посмотреть SQL

Включить echo или прочитать generated query в log.

### Изменить одно условие

Добавить только один optional filter или sort field.

### Проверить transaction

Для write operation подтвердить commit и generated values.

### Смоделировать ошибку

Нарушить unique constraint и убедиться, что выполнен rollback.

### контроль query-поведения

```text
$ pytest -q tests/test_tasks.py -k pagination
3 passed
$ pytest -q tests/test_tasks.py -k integrity
2 passed
```

## 6. Блок 16: связи и воспроизводимая схема

Одна таблица задач недостаточна для StudyHub. Категория и задачи создают первую one-to-many связь, а Alembic фиксирует изменения schema как историю, которую можно повторить на другой машине.

### Foreign key

**Метка:** `FK`.

Column tasks.category_id хранит ссылку на categories.id.

```text
ForeignKey("categories.id")
```

### Связанные объекты

**Метка:** `relationship`.

Python-атрибуты category.tasks и task.category упрощают навигацию.

```text
relationship(...)
```

### История schema

**Метка:** `Alembic`.

Revision описывает upgrade и downgrade между версиями структуры.

```text
alembic upgrade head
```

| Понятие | Смысл |
| --- | --- |
| `87 · Foreign key` | Category — parent, Task — child; база защищает ссылочную целостность. |
| `88 · Relationship` | ORM связывает объекты поверх foreign key. |
| `89 · Loading и N+1` | Повторяющиеся запросы замечаются по log и исправляются осознанной загрузкой. |
| `90 · Первая migration` | Alembic init, revision, upgrade и таблица alembic_version. |
| `91 · Изменение schema` | Новое поле проходит revision, upgrade, проверку и безопасный downgrade. |
| `92 · Итоговый проект` | Database API проходит tests, README, migrations, Postman и защиту. |

### One-to-many

```text
Category 1 ─────────── * Task

categories.id
      ↑
tasks.category_id
```

### Вопрос для объяснения

**Вопрос:** Почему одного relationship недостаточно для реальной связи таблиц?

Подсказка: Подумайте, какой механизм понимает сама SQLite без Python.

**Ориентир ответа:** Relationship помогает ORM работать с объектами, но ссылочная целостность базы опирается на foreign key column.

> Alembic не создаёт backup пользовательских данных и не заменяет внимательную migration strategy. Он хранит воспроизводимую историю изменений schema.

### Сначала foreign key

Создать реальную database-ссылку category_id → categories.id.

### Затем relationship

Добавить удобную навигацию между ORM-объектами.

### Потом наблюдать loading

Посчитать SQL queries при чтении categories и tasks.

### После измерения оптимизировать

Выбрать selectinload или другой подход для конкретного endpoint.

### Зафиксировать изменение

Создать migration revision и проверить upgrade.

### Проверить обратный путь

Выполнить downgrade на учебной базе и вернуть head.

### Безопасный migration rehearsal

```text
backup or disposable database
→ alembic current
→ alembic upgrade head
→ inspect schema
→ run tests
→ alembic downgrade -1
→ inspect schema
→ alembic upgrade head
```

## 7. Как будет выглядеть итоговый StudyHub

Финальный проект этапа остаётся небольшим и объяснимым. Он не маскирует базовые операции за десятками абстракций, но уже имеет устойчивые данные, relations, migrations и проверяемый API-контракт.

### Финальная структура проекта

```text
studyhub-database-api/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models/
│   │   ├── task.py
│   │   └── category.py
│   ├── schemas/
│   │   ├── task.py
│   │   └── category.py
│   ├── routers/
│   │   ├── tasks.py
│   │   └── categories.py
│   └── crud/
│       ├── tasks.py
│       └── categories.py
├── alembic/
├── tests/
├── .env.example
├── alembic.ini
└── README.md
```

### Стабильный контракт

**Метка:** `API`.

Endpoints, schemas, statuses, headers и CORS остаются понятными клиенту.

```text
GET /tasks
```

### Устойчивое состояние

**Метка:** `DB`.

SQLite tables, constraints и transactions переживают restart.

```text
studyhub.db
```

### Воспроизводимый проект

**Метка:** `quality`.

Alembic, tests, README и env example позволяют повторить запуск.

```text
alembic upgrade head
```

### Разбор сценариев

```text
request
→ middleware
→ dependency get_db
→ router
→ CRUD statement
→ Session transaction
→ ORM result
→ response schema
```

- `GET list` → строка 5 → `SELECT + 200`
- `POST valid` → строка 6 → `INSERT + COMMIT + 201`
- `duplicate value` → строка 6 → `ROLLBACK + 409/400`
- `unknown id` → строка 5 → `SELECT none + 404`

> Итоговая архитектура является учебной: она показывает границы и движение данных, не превращая проект в имитацию enterprise-системы.

### Демонстрационный сценарий итогового проекта

```text
1. alembic upgrade head
2. POST /categories → 201
3. POST /tasks with category_id → 201
4. GET /tasks?limit=2&offset=0 → 200
5. PATCH /tasks/{task_id} → 200
6. POST duplicate unique value → conflict + rollback
7. GET /categories/{category_id}/tasks → 200
8. restart API and repeat GET → rows remain
```

### Вопрос для объяснения

**Вопрос:** Почему перезапуск API входит в обязательную демонстрацию?

Подсказка: Сравните с in-memory storage третьего этапа.

**Ориентир ответа:** Он доказывает, что response строится из устойчивых database rows, а не из случайного Python-состояния текущего процесса.

### Запуск с нуля

Создать .env из example, установить dependencies и выполнить migrations.

### Демонстрация данных

Создать category и несколько tasks, затем перезапустить server.

### Демонстрация queries

Показать filters, pagination, count и unknown id.

### Демонстрация integrity

Создать конфликт и показать rollback без поломки следующего request.

### Демонстрация relations

Получить category с tasks без неконтролируемого N+1.

### Демонстрация истории

Показать alembic history и текущую revision.

### финальная защита

```text
$ alembic upgrade head
database schema is current
$ python -m pytest -q
all tests passed
$ python -m uvicorn app.main:app
StudyHub Database API is ready
```

## 8. Рабочий ритм и критерии готовности

Новый материал закрепляется не чтением, а коротким циклом: сформулировать ожидание, запустить, увидеть SQL или response, изменить одну деталь, повторить проверку и объяснить результат.

### Перед кодом

Назовите проблему текущей версии и результат нового механизма.

### Перед запуском

Предскажите request, SQL statement, transaction state или response status.

### После запуска

Проверьте server log, SQL echo, SQLite viewer, Swagger или test output.

### После изменения

Меняйте одно условие, column или query parameter за шаг.

### При ошибке

Определите слой: validation, dependency, connection, statement, constraint, commit или serialization.

### Перед коммитом

Запустите tests и убедитесь, что migration и README соответствуют коду.

### Обязательная контрольная матрица

```text
обычный request
пустая collection
unknown id
invalid input
duplicate unique value
failed commit + rollback
relation without parent
pagination boundary
migration upgrade
migration downgrade
```

### Правда или ложь

Если endpoint вернул 200 один раз, database layer уже считается проверенным.

**Ответ:** ложь. Нужны границы, ошибки constraints, rollback, restart, migrations и повторный request.

**Финальная проверка карты обучения:**

> Готовность означает способность объяснить путь данных от HTTP request до row в SQLite и обратно, а не только воспроизвести готовый код.

| Понятие | Смысл |
| --- | --- |
| `понять` | Объяснить назначение нового механизма без кода. |
| `увидеть` | Найти его место в request и database pipeline. |
| `запустить` | Получить наблюдаемый SQL, row, status или migration result. |
| `защитить` | Проверить normal path, boundary и ожидаемую ошибку. |

### Минимальная формула прогресса

```text
прочитал ≠ освоил
запустил ≠ понял
изменил + проверил + объяснил = рабочий навык
```

> Этап считается пройденным, когда ученик может восстановить пустую database, применить migrations, выполнить CRUD и объяснить transaction error без копирования готовой последовательности.

### Ритм одного занятия

```text
понять проблему
→ увидеть минимальный механизм
→ предсказать SQL или response
→ запустить
→ изменить одну деталь
→ проверить normal и error path
→ объяснить transaction state
→ зафиксировать commit
```

### Формулировка ожидания

**Метка:** `до`.

Какой SQL, status или schema change должен появиться?

### Наблюдение границы

**Метка:** `во время`.

Что происходит с Session, transaction и ORM object?

### Объяснение и защита

**Метка:** `после`.

Почему результат верен и какой test сохранит контракт?

## Итоговый квиз

1. **Почему блок 13 идёт до SQLite?**
   - A. `Сначала подготавливаются dependencies и configuration`
   - B. `SQLite запрещена в начале файла`
   - C. `Middleware создаёт таблицы`
   - **Ответ: A.** Get_db должен появиться в уже понятной модели request dependencies.

2. **Что является главным объектом блока 14?**
   - A. `engine, ORM model и Session lifecycle`
   - B. `JWT token`
   - C. `Redis queue`
   - **Ответ: A.** Блок вводит базовые части SQLAlchemy и первую запись.

3. **Что добавляет блок 15?**
   - A. `реальные queries и transaction errors`
   - B. `только новые folders`
   - C. `frontend components`
   - **Ответ: A.** CRUD развивается до filters, aggregates и rollback.

4. **Зачем нужен Alembic?**
   - A. `воспроизводить историю schema`
   - B. `хранить API cookies`
   - C. `заменить pytest`
   - **Ответ: A.** Migration revisions описывают изменения структуры базы.

## Главное

- Этап 4 развивает готовый Planner API, а не начинает новый проект.
- FastAPI dependencies и settings подготавливают подключение базы.
- SQLite даёт устойчивое локальное хранение без отдельного сервера.
- SQLAlchemy изучается через engine, model, Session и statement.
- CRUD дополняется filters, pagination, aggregates и transactions.
- Foreign key и relationship решают разные части связи.
- Alembic делает schema воспроизводимой.
- Итоговый проект должен объясняться от request до database row.

## Практика

Перед занятием 69 нарисуйте две схемы: текущий путь request в Planner API и будущий путь request через get_db, Session и SQLite.

### Критерий готовности

- Ученик объясняет модель своими словами.
- Может проследить один сценарий от начала до результата.
- Называет ограничение текущего решения.
- Понимает, какой следующий урок решает это ограничение.

<!-- youtube: -->
