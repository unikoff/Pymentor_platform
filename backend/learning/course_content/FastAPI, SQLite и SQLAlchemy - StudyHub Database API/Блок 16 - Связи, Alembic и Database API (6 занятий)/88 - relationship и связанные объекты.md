# 88 — relationship и связанные объекты

## Паспорт занятия

- **Блок:** 16 — Связи, Alembic и Database API
- **Сквозной проект:** StudyHub Database API
- **Формат:** теория, управляемые примеры, диагностика, проектная практика
- **Время:** 60–90 минут
- **До занятия:** ForeignKey уже хранит category_id.
- **После занятия:** ORM даёт task.category и category.tasks, API — конечный JSON.
- **Намеренно отложено:** стоимость загрузки relationship.

## Результат

Добавим ORM-навигацию поверх внешнего ключа: настроим relationship и back_populates, получим task.category и category.tasks, а затем спроектируем вложенные Pydantic-схемы без бесконечной рекурсии.

> **Связь с курсом.** ForeignKey уже хранит category_id и защищает ссылку. Теперь та же связь должна стать удобной для Python-кода и API-ответа.

> **Граница темы.** relationship не создаёт колонку и не заменяет ForeignKey. Он описывает навигацию ORM-объектов и стратегию их загрузки.

## Маршрут занятия

1. Соединить orm-атрибуты.
2. Синхронизировать объект и id.
3. Спроектировать краткую вложенную схему.
4. Проверить обе стороны связи.

Каждый шаг подтверждается кодом, SQL-логом, migration-командой или тестом.

## 1. Три уровня одной связи

В базе Task хранит число category_id. В Python разработчик хочет читать task.category.name. В HTTP-контракте клиенту нужен конечный JSON. Эти три уровня связаны, но каждый решает отдельную задачу.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Три уровня одной связи» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
ForeignKey("categories.id")
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
task.category
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 3

```python
TaskReadWithCategory
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «Три уровня одной связи» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Три уровня одной связи», какую проблему он решает и какое ограничение оставляет.

## 2. back_populates с двух сторон

back_populates связывает встречные атрибуты одной связи. Имя в строке должно точно совпасть с атрибутом другого ORM-класса.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «back_populates с двух сторон» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
from __future__ import annotations

from sqlalchemy.orm import Mapped, relationship


class CategoryModel(Base):
    __tablename__ = "categories"

    # id и name опущены только в этом фрагменте
    tasks: Mapped[list[TaskModel]] = relationship(
        back_populates="category",
    )


class TaskModel(Base):
    __tablename__ = "tasks"

    # id, title и category_id уже объявлены
    category: Mapped[CategoryModel | None] = relationship(
        back_populates="tasks",
    )
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
category = CategoryModel(name="Python")
task = TaskModel(title="ORM")
task.category = category
print(task.category.name)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «back_populates с двух сторон» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «back_populates с двух сторон», какую проблему он решает и какое ограничение оставляет.

## 3. Назначение объекта и синхронизация id

Связь можно установить числом category_id или объектом category. После flush SQLAlchemy синхронизирует внешний ключ. В базе всё равно сохраняется integer.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Назначение объекта и синхронизация id» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
category = CategoryModel(name="Python")
task = TaskModel(
    title="Relationship",
    category=category,
)

session.add(task)
session.commit()
session.refresh(task)

print(task.category_id)
print(task.category.name)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Как создать связанную задачу?
- Что находится в строке таблицы tasks?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Назначение объекта и синхронизация id», какую проблему он решает и какое ограничение оставляет.

## 4. Вложенные response schemas без рекурсии

Полные схемы Task и Category нельзя бесконечно вкладывать друг в друга. Для вложения создаётся краткая CategoryBrief, а обратный список использует TaskRead без повторной category.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Вложенные response schemas без рекурсии» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
from pydantic import BaseModel, ConfigDict


class CategoryBrief(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: int
    name: str


class TaskRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: int
    title: str
    category_id: int | None


class TaskReadWithCategory(TaskRead):
    category: CategoryBrief | None


class CategoryReadWithTasks(CategoryBrief):
    tasks: list[TaskRead]
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Какая структура безопасна?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Вложенные response schemas без рекурсии», какую проблему он решает и какое ограничение оставляет.

## 5. GET задачи со связанной категорией

Endpoint получает TaskModel и возвращает его по TaskReadWithCategory. Если category_id равен None, вложенное поле становится null.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «GET задачи со связанной категорией» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
@router.get(
    "/{task_id}",
    response_model=TaskReadWithCategory,
)
def get_task(
    task_id: int,
    session: Annotated[Session, Depends(get_db)],
):
    task = session.get(TaskModel, task_id)

    if task is None:
        raise HTTPException(
            status_code=404,
            detail="Task not found",
        )

    return task
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
task = TaskModel(id=1, title="Без категории", category_id=None)
print(task.category)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «GET задачи со связанной категорией» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «GET задачи со связанной категорией», какую проблему он решает и какое ограничение оставляет.

## 6. Обратная коллекция category.tasks

Список задач категории полезен, но может быть большим. Поэтому он выдаётся отдельным endpoint, а базовая карточка категории остаётся компактной.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Обратная коллекция category.tasks» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
@router.get(
    "/{category_id}/tasks",
    response_model=list[TaskRead],
)
def get_category_tasks(
    category_id: int,
    session: Annotated[Session, Depends(get_db)],
):
    category = session.get(CategoryModel, category_id)

    if category is None:
        raise HTTPException(
            status_code=404,
            detail="Category not found",
        )

    return category.tasks
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему список задач вынесен отдельно?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Обратная коллекция category.tasks», какую проблему он решает и какое ограничение оставляет.

## 7. Типичные ошибки relationship

Чаще всего ломаются имена back_populates или сериализация полного графа. Диагностику ведут по слоям: ForeignKey, оба relationship, затем Pydantic-схема.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Типичные ошибки relationship» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
class CategoryModel(Base):
    tasks = relationship(back_populates="category")


class TaskModel(Base):
    category = relationship(back_populates="items")
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
class CategoryModel(Base):
    tasks = relationship(back_populates="category")


class TaskModel(Base):
    category = relationship(back_populates="tasks")
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему mapper не может настроить связь?
- Расположите уровни от строки базы до JSON.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Типичные ошибки relationship», какую проблему он решает и какое ограничение оставляет.

## Контрольные вопросы с ответами

### 1. Что делает relationship?

- A. создаёт ORM-навигацию
- B. заменяет ForeignKey
- C. создаёт route

**Ответ:** A. Relationship предоставляет связанные объекты.

### 2. На что указывает back_populates?

- A. на встречный атрибут
- B. на имя базы
- C. на response schema

**Ответ:** A. Имя должно существовать в другом классе.

### 3. Зачем CategoryBrief?

- A. остановить вложение
- B. создать таблицу
- C. выполнить commit

**Ответ:** A. Краткая схема делает ответ конечным.

### 4. Что вернёт task.category без связи?

- A. None
- B. пустой dict
- C. ошибку всегда

**Ответ:** A. Nullable relationship допускает отсутствие родителя.

## Главное из занятия

- ForeignKey и relationship решают разные задачи.
- back_populates связывает встречные ORM-атрибуты.
- TaskModel.category возвращает объект или None.
- CategoryModel.tasks возвращает коллекцию.
- Pydantic читает ORM через from_attributes.
- Краткие схемы останавливают рекурсию.
- Большие коллекции лучше выдавать отдельным endpoint.

## Практическое задание

Добавьте двусторонний relationship, TaskReadWithCategory и GET /categories/{category_id}/tasks. Проверьте связанную и несвязанную задачу.

## Критерии проверки

- Проект запускается из чистого состояния по зафиксированным командам.
- Успешный сценарий подтверждён HTTP-запросом или тестом.
- Минимум два граничных сценария имеют ожидаемый статус и объяснение.
- Session после ожидаемой ошибки остаётся работоспособной.
- Ученик показывает место реализации каждого термина.
- В Git нет рабочей базы, секретов и временных логов.

## Объяснение наставнику

За две минуты объясните путь данных и одну ошибку, которую предотвращает урок. Затем покажите код, migration или тест, подтверждающий объяснение.

<!-- youtube: -->
