# 89 — Загрузка связей и первое N+1

## Паспорт занятия

- **Блок:** 16 — Связи, Alembic и Database API
- **Сквозной проект:** StudyHub Database API
- **Формат:** теория, управляемые примеры, диагностика, проектная практика
- **Время:** 60–90 минут
- **До занятия:** relationship работает функционально.
- **После занятия:** Список задач получает category двумя запросами.
- **Намеренно отложено:** глубокий профайлинг SQL.

## Результат

Увидим скрытую стоимость relationship: включим SQL-логи, воспроизведём N+1 на списке задач и исправим его через selectinload, не превращая оптимизацию в преждевременную магию.

> **Связь с курсом.** relationship уже даёт task.category. Теперь нужно понять, что удобный атрибут может выполнить дополнительный SQL-запрос.

> **Граница темы.** N+1 — конкретный шаблон: один запрос списка плюс дополнительные запросы связанных данных, а не название любого медленного endpoint.

## Маршрут занятия

1. Увидеть повторяющиеся select.
2. Зафиксировать исходный лог.
3. Добавить selectinload в нужный запрос.
4. Повторить тот же сценарий.

Каждый шаг подтверждается кодом, SQL-логом, migration-командой или тестом.

## 1. Удобный атрибут может скрывать SQL

task.category.name выглядит как чтение обычного атрибута. Если category не загружена, SQLAlchemy может обратиться к базе. На одной записи это незаметно, а на списке превращается в поток запросов.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Удобный атрибут может скрывать SQL» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```sql
SELECT ... FROM tasks
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```sql
SELECT ... FROM categories WHERE id = ?
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 3

```sql
WHERE categories.id IN (...)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «Удобный атрибут может скрывать SQL» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Удобный атрибут может скрывать SQL», какую проблему он решает и какое ограничение оставляет.

## 2. Как возникает 1 + N

Сначала выполняется один SELECT задач. Затем цикл или сериализация обращается к category каждой задачи. В худшем случае это N дополнительных SELECT.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Как возникает 1 + N» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
statement = select(TaskModel)
tasks = session.scalars(statement).all()

for task in tasks:
    print(task.category.name)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
# Четыре задачи относятся к четырём разным категориям.
# Связи загружаются лениво.
print(1 + 4)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему реальное число иногда меньше N?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Как возникает 1 + N», какую проблему он решает и какое ограничение оставляет.

## 3. SQL-логи вместо догадок

echo=True выводит SQLAlchemy-запросы в терминал. На учебном проекте этого достаточно, чтобы увидеть повторяющиеся SELECT categories.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «SQL-логи вместо догадок» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
engine = create_engine(
    settings.database_url,
    echo=True,
)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Что искать в логе?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «SQL-логи вместо догадок», какую проблему он решает и какое ограничение оставляет.

## 4. selectinload: два предсказуемых запроса

selectinload сначала загружает задачи, собирает category_id и отдельным SELECT получает все нужные категории через IN.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «selectinload: два предсказуемых запроса» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```text
from sqlalchemy import select
from sqlalchemy.orm import selectinload


def list_tasks_with_categories(
    session: Session,
) -> list[TaskModel]:
    statement = (
        select(TaskModel)
        .options(
            selectinload(TaskModel.category)
        )
        .order_by(TaskModel.id)
    )
    return session.scalars(statement).all()
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
queries_before = 6
queries_after = 2
print(queries_before - queries_after)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «selectinload: два предсказуемых запроса» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «selectinload: два предсказуемых запроса», какую проблему он решает и какое ограничение оставляет.

## 5. selectinload против joinedload

joinedload делает JOIN в основном SELECT, selectinload — отдельный запрос. Универсального победителя нет. Для первого списка StudyHub selectinload проще наблюдать.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «selectinload против joinedload» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Интерактив в текстовой форме

- Что выбрать для списка задач с категорией?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «selectinload против joinedload», какую проблему он решает и какое ограничение оставляет.

## 6. Endpoint списка без скрытых запросов

response_model читает category, поэтому CRUD-функция обязана вернуть задачи с подготовленной связью. Стратегия находится рядом с select.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Endpoint списка без скрытых запросов» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
@router.get(
    "",
    response_model=list[TaskReadWithCategory],
)
def list_tasks(
    session: Annotated[
        Session,
        Depends(get_db),
    ],
):
    statement = (
        select(TaskModel)
        .options(
            selectinload(TaskModel.category)
        )
        .order_by(TaskModel.id)
    )
    return session.scalars(statement).all()
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему order_by(TaskModel.id) полезен?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Endpoint списка без скрытых запросов», какую проблему он решает и какое ограничение оставляет.

## 7. Диагностика и исправление N+1

API-тест формы ответа не гарантирует разумное число SQL-запросов. На обязательном уровне достаточно повторяемого сценария с echo и сравнением лога.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Диагностика и исправление N+1» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
def list_tasks(session):
    tasks = session.scalars(
        select(TaskModel)
    ).all()

    return [
        {
            "id": task.id,
            "category": task.category.name,
        }
        for task in tasks
    ]
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
def list_tasks(session):
    statement = select(TaskModel).options(
        selectinload(TaskModel.category)
    )
    return session.scalars(statement).all()
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Где возникает риск N+1?
- Расположите путь от наблюдения к проверенному исправлению.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Диагностика и исправление N+1», какую проблему он решает и какое ограничение оставляет.

## Контрольные вопросы с ответами

### 1. Что означает 1 в N+1?

- A. основной SELECT
- B. одна модель
- C. одна колонка

**Ответ:** A. Сначала загружается основной набор.

### 2. Где проявляется N+1?

- A. при чтении relationship в цикле
- B. при объявлении Base
- C. в .env

**Ответ:** A. Повторная ленивая загрузка создаёт SELECT.

### 3. Что делает selectinload?

- A. загружает связи отдельным IN
- B. удаляет ForeignKey
- C. создаёт migration

**Ответ:** A. Связанные строки приходят одним набором.

### 4. Нужно ли eager loading каждому SELECT?

- A. нет
- B. да
- C. только INSERT

**Ответ:** A. Он нужен только для конкретного контракта ответа.

## Главное из занятия

- Relationship может выполнить SQL при чтении.
- N+1 — основной запрос плюс повторяющиеся загрузки.
- SQL-лог показывает реальное поведение ORM.
- selectinload обычно превращает 1+N в два запроса.
- Стратегия загрузки задаётся рядом с select.
- Response schema может неявно читать relationship.
- Оптимизацию проверяют тем же сценарием.

## Практическое задание

Включите echo, воспроизведите N+1 в GET /tasks, добавьте selectinload и сохраните в README фрагменты лога до и после.

## Критерии проверки

- Проект запускается из чистого состояния по зафиксированным командам.
- Успешный сценарий подтверждён HTTP-запросом или тестом.
- Минимум два граничных сценария имеют ожидаемый статус и объяснение.
- Session после ожидаемой ошибки остаётся работоспособной.
- Ученик показывает место реализации каждого термина.
- В Git нет рабочей базы, секретов и временных логов.

## Объяснение наставнику

За две минуты объясните путь данных и одну ошибку, которую предотвращает урок. Затем покажите код, migration или тест, подтверждающий объяснение.
