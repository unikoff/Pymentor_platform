# 92 — Итоговый проект этапа 4

## Паспорт занятия

- **Блок:** 16 — Связи, Alembic и Database API
- **Сквозной проект:** StudyHub Database API
- **Формат:** теория, управляемые примеры, диагностика, проектная практика
- **Время:** 60–90 минут
- **До занятия:** Все механизмы этапа изучены.
- **После занятия:** Database API воспроизводится с чистой базы.
- **Намеренно отложено:** users, auth, JWT, Docker и async.

## Результат

Соберём StudyHub Database API как законченный синхронный сервис: сведём роутеры, схемы, ORM-модели, CRUD, связи и Alembic, подключим отдельную тестовую SQLite-базу и подготовим проект к защите.

> **Связь с курсом.** Предыдущие блоки дали отдельные механизмы FastAPI, SQLAlchemy и Alembic. Теперь они должны образовать один объяснимый путь запроса.

> **Граница темы.** Финальный проект не добавляет пользователей, JWT или async. Эти темы относятся к следующему этапу.

## Маршрут занятия

1. Создать схему с нуля.
2. Запустить тесты отдельно от рабочей бд.
3. Показать связанный http-сценарий.
4. Защитить архитектурные решения.

Каждый шаг подтверждается кодом, SQL-логом, migration-командой или тестом.

## 1. Что считается завершённым API

Новый разработчик должен клонировать репозиторий, применить migrations, запустить тесты и увидеть одинаковое поведение. Однократный запуск у автора не является готовностью.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Что считается завершённым API» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
routers + schemas
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
models + crud
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 3

```python
migrations + tests
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «Что считается завершённым API» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Что считается завершённым API», какую проблему он решает и какое ограничение оставляет.

## 2. Структура и ответственности

Папки разделены по причинам изменения. Новая абстракция добавляется только при реальной необходимости, а не ради архитектурного декора.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Структура и ответственности» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```text
studyhub/
├── alembic.ini
├── migrations/
│   ├── env.py
│   └── versions/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models/
│   ├── schemas/
│   ├── crud/
│   └── routers/
├── tests/
│   ├── conftest.py
│   ├── test_categories.py
│   └── test_tasks.py
├── .env.example
├── pyproject.toml
└── README.md
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему не добавляется service layer автоматически?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Структура и ответственности», какую проблему он решает и какое ограничение оставляет.

## 3. Полный путь POST /tasks

Ученик должен объяснить запрос от JSON до строки базы и обратно, не читая все файлы подряд.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Полный путь POST /tasks» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
@router.post(
    "",
    response_model=TaskReadWithCategory,
    status_code=201,
)
def create_task_endpoint(
    data: TaskCreate,
    session: Annotated[
        Session,
        Depends(get_db),
    ],
):
    return tasks_crud.create_task(
        session,
        data,
    )
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
steps = [
    "validation",
    "session",
    "crud",
    "commit",
    "response",
]
print(len(steps))
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «Полный путь POST /tasks» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Полный путь POST /tasks», какую проблему он решает и какое ограничение оставляет.

## 4. Отдельная тестовая SQLite-база

Тесты не должны читать или менять рабочий studyhub.db. get_db подменяется, а in-memory SQLite использует StaticPool, чтобы приложение и TestClient видели одно подключение.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Отдельная тестовая SQLite-база» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from app.database import Base, get_db
from app.main import app

engine = create_engine(
    "sqlite://",
    connect_args={
        "check_same_thread": False,
    },
    poolclass=StaticPool,
)

TestingSessionLocal = sessionmaker(
    bind=engine,
    expire_on_commit=False,
)


@pytest.fixture
def client():
    Base.metadata.create_all(engine)

    def override_get_db():
        with TestingSessionLocal() as session:
            yield session

    app.dependency_overrides[
        get_db
    ] = override_get_db

    with TestClient(app) as test_client:
        yield test_client

    app.dependency_overrides.clear()
    Base.metadata.drop_all(engine)
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Зачем StaticPool?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Отдельная тестовая SQLite-база», какую проблему он решает и какое ограничение оставляет.

## 5. Матрица обязательных тестов

Тесты выбираются по рискам: успешные сценарии, границы данных, связи, конфликты и восстановление Session.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Матрица обязательных тестов» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
def test_create_task_with_category(client):
    category_response = client.post(
        "/categories",
        json={"name": "Python"},
    )
    category_id = (
        category_response.json()["id"]
    )

    response = client.post(
        "/tasks",
        json={
            "title": "Relationships",
            "category_id": category_id,
        },
    )

    assert response.status_code == 201
    assert (
        response.json()["category"]["name"]
        == "Python"
    )
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Почему после IntegrityError нужен ещё один запрос?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Матрица обязательных тестов», какую проблему он решает и какое ограничение оставляет.

## 6. README и запуск с чистого состояния

README является исполняемой инструкцией: установка, .env, migrations, тесты, сервер и примеры запросов.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «README и запуск с чистого состояния» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```text
## Запуск

python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
alembic upgrade head
uvicorn app.main:app --reload

## Проверка

pytest
alembic current
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```bash
python -m venv .venv
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 3

```bash
cp .env.example .env
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 4

```bash
http GET :8000/tasks
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Объясните смысл раздела «README и запуск с чистого состояния» своими словами.
- Назовите корректный и ошибочный сценарий.
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «README и запуск с чистого состояния», какую проблему он решает и какое ограничение оставляет.

## 7. Защита и границы следующего этапа

На защите ученик показывает воспроизводимость, связанный сценарий и объясняет ключевые пары понятий. Новые технологии не добавляются в последний момент.

### Точная модель

- **Контекст:** StudyHub уже находится в состоянии, описанном в паспорте занятия.
- **Механизм:** раздел «Защита и границы следующего этапа» добавляет одну новую техническую идею.
- **Контракт:** до выполнения называются вход, ожидаемый результат и допустимые ошибки.
- **Граница:** механизм не подменяет ответственность соседнего слоя.

### Пример 1

```python
@pytest.fixture
def client():
    return TestClient(app)

# get_db использует рабочий DATABASE_URL
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Пример 2

```python
@pytest.fixture
def client():
    app.dependency_overrides[get_db] = override_get_db

    with TestClient(app) as test_client:
        yield test_client

    app.dependency_overrides.clear()
```

Пошаговый разбор:

1. Назовите данные и объекты, существующие до первой строки.
2. Укажите строку, где меняется состояние, строится запрос или задаётся связь.
3. Предскажите результат до запуска.
4. Измените одно значение и объясните новый результат.

### Интерактив в текстовой форме

- Расположите демонстрацию от чистой базы до объяснения.
- Почему тест опасен?
- Найдите место ожидаемой ошибки и назовите её техническую причину.
- Сформулируйте проверяемый критерий готовности раздела.

### Проверка раздела

Объясните, зачем StudyHub нужен раздел «Защита и границы следующего этапа», какую проблему он решает и какое ограничение оставляет.

## Контрольные вопросы с ответами

### 1. Что создаёт схему на чистой базе?

- A. alembic upgrade head
- B. первый endpoint
- C. response_model

**Ответ:** A. Migration history является путём схемы.

### 2. Зачем override get_db?

- A. дать тестовую Session
- B. изменить method
- C. создать relationship

**Ответ:** A. Тесты изолируются от рабочей базы.

### 3. Где находится SQL списка?

- A. crud
- B. schema
- C. README

**Ответ:** A. CRUD управляет Session и select.

### 4. Что отложено?

- A. пользователи и authentication
- B. ForeignKey
- C. Alembic

**Ответ:** A. Безопасность начинается в следующем этапе.

## Главное из занятия

- StudyHub Database API является цельным синхронным приложением.
- Слои разделены по причинам изменения.
- Session создаётся на запрос и подменяется в тестах.
- Связи защищены ForeignKey и доступны через relationship.
- N+1 диагностируется логом и исправляется явной загрузкой.
- Alembic воспроизводит и развивает схему.
- README и тесты делают проект проверяемым.

## Практическое задание

Соберите финальный StudyHub Database API, прогоните его с чистой базы и подготовьте семиминутную защиту: migrations, tests, связанный POST/GET и объяснение архитектуры.

## Критерии проверки

- Проект запускается из чистого состояния по зафиксированным командам.
- Успешный сценарий подтверждён HTTP-запросом или тестом.
- Минимум два граничных сценария имеют ожидаемый статус и объяснение.
- Session после ожидаемой ошибки остаётся работоспособной.
- Ученик показывает место реализации каждого термина.
- В Git нет рабочей базы, секретов и временных логов.

## Объяснение наставнику

За две минуты объясните путь данных и одну ошибку, которую предотвращает урок. Затем покажите код, migration или тест, подтверждающий объяснение.
