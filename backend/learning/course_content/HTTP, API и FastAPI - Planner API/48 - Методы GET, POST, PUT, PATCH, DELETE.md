# 48. Методы GET, POST, PUT, PATCH, DELETE

<!-- youtube: -->

## 00. Один request может означать разные действия

В прошлом занятии мы разобрали, из чего состоит request. Клиент указывает адрес, выбирает method, добавляет служебные headers и при необходимости передаёт body. В следующем занятии мы начнём собирать более целостный договор API.

Между этими двумя темами нужен ещё один слой. Одного адреса недостаточно, чтобы понять намерение клиента. Запрос к одной и той же коллекции может:

- попросить список;
- создать новую задачу;
- заменить уже существующую задачу;
- изменить только один параметр;
- удалить задачу.

Представьте шкаф с папкой заявлений. Адрес шкафа говорит, куда пришли. Но сама записка должна сообщить, что сделать: показать папку, добавить лист, заменить лист, поправить одну строку или убрать лист. В HTTP эту роль выполняет method.

~~~text
адрес → с каким объектом работаем
method → какое действие просим выполнить
headers → как понимать сообщение
body → какие данные передаём
~~~

### Небольшой прогноз

Посмотрите на обычную Python-функцию:

~~~python
def read_title(task):
    return task["title"]

task = {"title": "Изучить HTTP"}
print(read_title(task))
~~~

**Что напечатает код?**

Ответьте до чтения дальше. Здесь функция только читает значение. Она не меняет словарь и не создаёт новую задачу. Это полезная модель для GET: сначала понять действие, а потом выбрать сетевую форму.

### Что добавляется сейчас

Мы разберём пять стандартных методов:

- GET для чтения;
- POST для создания или передачи новой операции на обработку;
- PUT для полной замены представления;
- PATCH для частичного изменения;
- DELETE для удаления.

Отдельно разберём два свойства, которые часто встречаются в документации и обсуждениях production-систем:

- safety, то есть отсутствие намеренного изменения состояния;
- idempotency, то есть сохранение одного и того же итогового состояния при повторе одинакового запроса.

### Чего пока не будет

В этой теме не нужны:

- декораторы маршрутов;
- FastAPI;
- параметры адреса;
- база данных;
- настоящая отправка запросов;
- реализация CRUD-сервиса.

Сначала нужно научиться видеть смысл метода. Потом этот смысл будет привязан к ресурсу, адресу и коду сервера.

## 01. Method является глаголом договора

Method можно представить как глагол в короткой фразе. Адрес называет объект, а method говорит, что с ним сделать.

~~~text
GET    задачи       → прочитать задачи
POST   задачи       → создать задачу
PUT    задача 7     → заменить задачу 7 целиком
PATCH  задача 7     → изменить часть задачи 7
DELETE задача 7     → удалить задачу 7
~~~

Здесь важно не заучить пять английских слов отдельно, а увидеть причинную связь:

1. Клиент выбирает объект или группу объектов.
2. Клиент формулирует намерение через method.
3. Сервер проверяет, разрешено ли такое действие.
4. Сервер возвращает response с результатом.

### Один адрес, разный смысл

Один и тот же адрес может принимать разные методы. Это не противоречие. Адрес отвечает на вопрос «где находится область работы», а method отвечает на вопрос «что сделать».

~~~text
GET  /tasks → прочитать коллекцию
POST /tasks → попросить создать новую Task
~~~

Если бы мы записывали действие только в тексте адреса, получилось бы много похожих вариантов:

~~~text
/tasks/list
/tasks/create
/tasks/update
/tasks/delete
~~~

Такой стиль иногда встречается в старых или внутренних системах, но стандартные HTTP methods дают клиентам более единый язык.

### Method не равен имени Python-функции

На сервере могут быть функции list_tasks, create_task и update_task. Это внутренние имена. Клиент не вызывает их напрямую.

~~~text
клиент выбирает method
    ↓
сервер находит подходящее правило
    ↓
внутри может вызвать Python-функцию
    ↓
сервер формирует response
~~~

Поэтому нельзя выбирать method только по названию обработчика. Сначала определите смысл операции. Название внутренней функции может быть любым.

### Интерактив. Найдите глагол

Для каждой фразы назовите действие обычными словами:

1. «Покажи все задачи».
2. «Добавь новую задачу».
3. «Замени карточку задачи полной новой карточкой».
4. «Поменяй только флаг готовности».
5. «Убери задачу из списка».

Проверьте себя по порядку: чтение, создание, полная замена, частичное изменение, удаление. В следующих разделах мы разберём, почему для этих смыслов используются разные methods.

### Где проходит граница

Method описывает намерение на внешней границе. Он не говорит, как именно сервер хранит данные. После GET сервер может читать JSON-файл, таблицу базы данных или кэш. После DELETE он может физически удалить запись или пометить её удалённой по внутренней политике.

Для клиента важен устойчивый внешний договор, а не конкретная технология внутри.

## 02. GET: прочитать текущее состояние

GET предназначен для получения представления ресурса или коллекции. Слово «представление» здесь означает данные в форме, которую сервер отдаёт клиенту. Это может быть список задач, одна задача или сводка.

~~~text
GET /tasks
→ попросить список задач

GET /tasks/7
→ попросить данные задачи с id 7
~~~

### Почему GET не должен изменять данные

Пользователь может открыть страницу несколько раз. Браузер может повторить запрос после временной сетевой ошибки. Прокси или кэш могут попросить данные снова. Если каждое чтение неожиданно меняет задачу, результат зависит от технических повторов, а не от действия пользователя.

Хорошая модель выглядит так:

~~~text
GET → прочитать → вернуть response
GET → прочитать → вернуть тот же актуальный результат
~~~

Повторный GET не должен:

- отмечать задачу выполненной;
- удалять запись;
- создавать новую запись;
- увеличивать пользовательский счётчик только из-за факта чтения.

Логи, метрики и счётчик просмотров на сервере могут обновляться как технические побочные эффекты. Когда говорят, что GET безопасен, имеют в виду отсутствие намеренного изменения предметного состояния ресурса.

### Независимый пример

Сервис расписания получает список свободных аудиторий:

~~~text
GET /rooms/free
→ список аудиторий
~~~

Запрос не бронирует аудиторию. Для бронирования нужен другой смысл и другой contract. Клиент может обновлять экран расписания, не опасаясь создать бронь случайным повтором чтения.

### GET и body

Для простого чтения body обычно не нужен. Всё необходимое для поиска позже можно разместить в адресе запроса или в query-параметрах. Но сейчас нам важнее не синтаксис адреса, а правило: GET сообщает о чтении.

Не делайте из отсутствия body абсолютный закон. Формат конкретного API может разрешать дополнительные данные. Однако body не превращает GET в метод изменения. Смысл определяется операцией и договором.

### Production-контекст

GET часто кэшируется, повторяется и вызывается разными клиентами. Это делает его удобным для:

- загрузки страниц;
- получения списков;
- поиска данных;
- проверки текущего состояния;
- работы мобильного приложения при повторном открытии экрана.

Если GET запускает изменение, система становится непредсказуемой. Например, ссылка «/mark-read» в виде GET может сработать из предпросмотра сообщения, поискового робота или фоновой проверки ссылки.

### Интерактив. Что напечатает код?

~~~python
task = {"title": "Прочитать договор", "is_done": False}

def get_task(task):
    return task.copy()

first = get_task(task)
second = get_task(task)
print(task["is_done"], first == second, first is second)
~~~

Сначала предскажите три значения. Затем объясните, что здесь повторяется чтение, а не изменение исходного словаря. Ожидаемый вывод: False True False.

### С чем путают GET

GET иногда путают с «самым простым методом, значит им можно сделать всё». Это опасная привычка. Удобство вызова не заменяет смысл. Если операция меняет состояние, нужно выбрать method изменения и описать его contract.

## 03. POST: создать новый ресурс или передать новую операцию

POST используется, когда клиент передаёт серверу новую информацию для обработки. Частый сценарий в нашем проекте: создать новую задачу внутри коллекции.

~~~text
POST /tasks
body: данные новой задачи
→ сервер проверяет данные
→ сервер назначает id
→ сервер возвращает созданную Task
~~~

### Почему POST подходит для создания

До создания у новой задачи ещё нет серверного id. Клиент обращается к области, где должны появиться задачи, и передаёт данные. Сервер решает:

- допустимы ли значения;
- какой id назначить;
- какие значения выставить по умолчанию;
- как сохранить результат;
- какой response вернуть.

Это похоже на заявление в регистратуре. Человек сообщает сведения, а система регистрирует новую запись и выдаёт номер.

### POST не обещает безопасный повтор

Два одинаковых POST обычно означают две попытки создать запись:

~~~text
POST «создай задачу SQL»
POST «создай задачу SQL»
→ могут появиться две разные задачи
~~~

Это не обязательно ошибка самого HTTP. Возможно, пользователь действительно дважды нажал кнопку. Но сетевой клиент может повторить отправку из-за таймаута, не зная, успел ли сервер создать запись.

В production для денежных операций, заказов и других чувствительных действий используют idempotency key, уникальный внешний идентификатор или иной договор повторной отправки. Эти механизмы будут изучаться отдельно. Сейчас достаточно увидеть причину: создание нового объекта не должно молча считаться безопасным повтором.

### POST и уже известный id

Обычно создание отправляется в коллекцию, потому что id назначает сервер. Если система принимает внешний id, это отдельное правило и его нужно явно описать. Нельзя делать вывод «POST всегда сам выбирает id» для всех API, но для учебного Planner это наша модель.

### Production-контекст

POST подходит не только для создания Task. В реальных системах он может:

- зарегистрировать пользователя;
- создать заказ;
- отправить комментарий;
- запустить длительную обработку;
- принять событие от другого сервиса.

Общая идея одна: клиент передаёт новую работу или новую сущность, а сервер принимает решение, как создать результат.

### Интерактив. Найдите риск повтора

Есть кнопка «Создать заказ». Пользователь нажал её один раз, но ответ не пришёл из-за обрыва сети. Клиент отправляет тот же POST второй раз.

Ответьте:

1. Почему клиент не может по одному таймауту понять, был ли создан заказ?
2. Почему два заказа могут быть опаснее, чем два чтения?
3. Какой вопрос нужно добавить в production-договор до реализации повтора?

Наводка: нужно спросить, как система узнаёт, что две попытки относятся к одной операции.

### С чем путают POST

POST путают с универсальным «сделать действие». Иногда это допустимо для командных API, но для ресурсоориентированного интерфейса сначала проверьте, не описывает ли операция создание, полное изменение, частичное изменение или удаление. Чем точнее method, тем проще клиенту предсказать response.

## 04. PUT: заменить полное представление

PUT выражает замену ресурса целиком согласно договору API. Клиент сообщает серверу, каким должно стать полное редактируемое представление.

Представьте анкету на бумаге. Если вы говорите «замените анкету новой версией», система должна получить всю форму, а не только одну строку. Иначе непонятно, случайно ли остальные поля пропущены или их нужно стереть.

### Механика полной замены

~~~text
текущее представление
    ↓ PUT с полной формой
новое представление ресурса
~~~

Пусть у Task есть такие поля:

~~~text
title
priority
is_done
tags
~~~

Полная замена может передать все четыре поля:

~~~text
PUT
title: «Изучить HTTP»
priority: 4
is_done: false
tags: [«backend»]
~~~

Что произойдёт с отсутствующим полем, зависит от contract. В одном API оно считается ошибкой. В другом получает значение по умолчанию. В третьем отсутствие означает удаление значения. Поэтому слова «полная замена» не отменяют необходимость чётких правил.

### Почему PUT считается идемпотентным

Если один и тот же полный документ отправить два раза, итоговое состояние должно быть тем же, что после одного такого PUT:

~~~text
PUT → Task стала полной формой X
PUT → Task снова стала полной формой X
итог → Task имеет форму X
~~~

Это не означает, что в логах будет одна запись или что сервер дважды ничего не делает. Речь идёт об итоговом состоянии ресурса.

### Независимый пример

Сервис профилей получает полную карточку настроек:

~~~text
PUT /profiles/12
language: «ru»
theme: «dark»
notifications: true
~~~

Если клиент отправляет эту же полную карточку повторно, профиль остаётся с теми же настройками.

### Production-контекст

PUT полезен, когда:

- клиент действительно владеет полной формой;
- объект можно описать целиком;
- повторная отправка должна приводить к одному итоговому состоянию;
- серверу удобно заменить одну версию представления другой.

Он менее удобен, когда форма большая, а пользователь меняет одно поле. Передача всей карточки увеличивает риск потерять изменения, сделанные другим клиентом между чтением и обновлением.

### Интерактив. Полная ли это форма?

У Task есть поля title, priority, is_done и tags. Сравните два body:

~~~text
A: {title, priority, is_done, tags}
B: {is_done}
~~~

Ответьте:

1. Какой body ближе к PUT?
2. Какой вопрос нужно задать contract, если пришёл только is_done?
3. Почему нельзя молча назвать B полной заменой?

Правильная мысль: B сообщает одно изменение и больше похож на PATCH. Но окончательное правило задаёт договор конкретного API.

### С чем путают PUT

PUT путают с PATCH, потому что оба меняют существующий объект. Различие не в том, что один «сильнее», а другой «слабее». Различие в объёме представления: PUT описывает полную замену, PATCH описывает изменение части.

## 05. PATCH: изменить часть представления

PATCH нужен, когда клиент не хочет передавать всю карточку. Он сообщает только те поля, которые нужно изменить, а остальные должны сохраниться по contract.

~~~text
текущая Task:
title: «Изучить HTTP»
priority: 4
is_done: false
tags: [«backend»]

PATCH:
is_done: true

результат:
title: «Изучить HTTP»
priority: 4
is_done: true
tags: [«backend»]
~~~

### Как сервер понимает PATCH

Упрощённая причинная цепочка выглядит так:

1. Сервер получает частичный набор полей.
2. Проверяет, что эти поля разрешены для изменения.
3. Проверяет типы и значения.
4. Находит текущий ресурс.
5. Накладывает изменения на текущую форму.
6. Сохраняет новую форму и возвращает response.

PATCH не означает «разрешить любые ключи». Если клиент передаст неизвестное поле или попытается изменить серверный id, contract должен отклонить такой request.

### Почему PATCH удобен для интерфейса

В приложении пользователь часто меняет одну настройку: флаг готовности, приоритет, название. Отправлять всю карточку каждый раз не обязательно. Частичный request:

- меньше по размеру;
- проще сформировать на экране;
- меньше повторяет уже известные данные;
- явно показывает, что именно меняется.

Но маленький body не отменяет конкуренцию изменений. Если два клиента почти одновременно меняют разные поля, серверу нужен понятный contract объединения и порядок обработки.

### PATCH и null

Отсутствие поля и значение null могут означать разные вещи:

~~~text
поле отсутствует → не менять
поле есть и равно null → очистить, если contract это разрешает
~~~

Это важная граница частичного обновления. Нельзя автоматически считать отсутствие и null одним и тем же. В обычной Python-функции различие можно выразить через наличие ключа. Во внешнем API это должно быть закреплено документацией и валидацией.

### PATCH бывает разным по формату

В production существуют разные соглашения о patch-документах. Одни передают словарь изменяемых полей, другие описывают список операций add, remove, replace. Для нашего учебного проекта достаточно простой модели: словарь с разрешёнными изменениями. Не нужно вводить более сложный формат, пока не возникла его задача.

### Интерактив. Что сохранится?

Текущая запись:

~~~text
title: «HTTP methods»
priority: 3
is_done: false
tags: [«api»]
~~~

Пришёл PATCH только с priority: 5.

Запишите без кода, какими станут все четыре поля. Затем ответьте, почему сервер не должен заменить отсутствующие title, is_done и tags на пустые значения.

Наводка: PATCH сообщает список намеренных изменений, а не новую полную карточку.

### С чем путают PATCH

PATCH путают с локальным присваиванием без проверки:

~~~python
task.update(incoming)
~~~

Такой код может быть частью реализации, но сам по себе не является безопасным контрактом. Сервис должен отдельно проверить разрешённые поля, типы, бизнес-правила и наличие ресурса. HTTP method задаёт смысл внешней операции, а не отменяет серверную валидацию.

## 06. DELETE: попросить удалить ресурс

DELETE выражает намерение удалить конкретный ресурс или убрать его из доступного представления.

~~~text
DELETE /tasks/7
→ сервер находит Task 7
→ применяет политику удаления
→ возвращает response
~~~

### Удаление не всегда означает физическое стирание

В учебном JSON-файле можно удалить объект из списка. В production система может выбрать:

- физическое удаление строки;
- soft delete через флаг;
- перенос в архив;
- событие для отдельного сервиса хранения.

Снаружи клиенту нужно сообщить, считается ли ресурс удалённым. Внутренний способ хранения является отдельной архитектурной деталью.

### Что делать при повторном DELETE

После первого успешного удаления ресурс отсутствует. Если тот же запрос приходит снова, возможны разные договоры:

~~~text
первый DELETE → 204
второй DELETE → 404
~~~

или:

~~~text
первый DELETE → 204
второй DELETE → 204
~~~

Второй вариант делает итоговое состояние устойчивым: после запроса объект отсутствует в обоих случаях. Но это не значит, что все системы обязаны возвращать одинаковый status. Нужна явная политика, которую понимает клиент.

### Production-контекст

Удаление требует особой осторожности:

- нужны права пользователя;
- действие может быть необратимым;
- иногда требуется подтверждение;
- полезно вести аудит;
- связанные объекты могут потребовать отдельного правила.

Method сам по себе не проверяет права и не гарантирует восстановление. Он только выражает смысл request.

### Интерактив. Найдите ошибку в ожидании

Разработчик пишет: «Если DELETE вернул 204, значит файл на диске точно уничтожен навсегда».

Почему это слишком сильное утверждение? Назовите минимум две причины. Подсказка: подумайте о soft delete, архиве и внешнем contract response.

## 07. Safety и idempotency: два похожих, но разных вопроса

Теперь соберём два свойства в одну картину. Они отвечают на разные вопросы.

### Safety

Безопасный method не должен по своему назначению изменять состояние ресурса.

Вопрос для проверки:

> Если клиент только хочет узнать текущее состояние, может ли сам факт запроса изменить задачу?

Для GET ответ должен быть «нет» в смысле предметного состояния.

Safety не означает отсутствие всех технических эффектов. Сервер всё ещё может записать лог, собрать метрику или обновить внутренний кэш.

### Idempotency

Идемпотентный contract сохраняет один и тот же итоговый state, если одинаковый request применить один или несколько раз.

Вопрос для проверки:

> Если сеть повторит тот же request, будет ли итоговое состояние таким же, как после одной обработки?

Это свойство про итог, а не про количество запусков кода, строк в логе или событий аудита.

### Сравнение методов

| Method | Намерение | Safe по смыслу ресурса | Idempotent в обычной модели |
| --- | --- | --- | --- |
| GET | прочитать | да | да |
| POST | создать или принять новую работу | нет | обычно нет |
| PUT | полностью заменить | нет | да |
| PATCH | частично изменить | нет | зависит от contract |
| DELETE | удалить | нет | обычно да для итогового отсутствия |

Слово «обычно» здесь важно. Например, сервер может при каждом DELETE запускать отдельное неповторяемое действие. Поэтому нужно смотреть на contract, а не только на название method.

### Четыре ситуации

~~~text
GET  → safe и idempotent
POST → не safe, обычно не idempotent
PUT  → не safe, idempotent
PATCH → не safe, idempotency зависит от операции
~~~

DELETE часто относят к idempotent, потому что после первого удаления ресурс уже отсутствует. Но повторный response может отличаться. «Один итог» и «одинаковый status» не всегда одно и то же.

### Интерактив. Выберите свойство

Для каждой фразы выберите safety, idempotency или оба свойства:

1. «Запрос списка не должен менять задачу».
2. «Повторная полная замена оставляет тот же документ».
3. «Два создания могут добавить две записи».
4. «Повторное частичное увеличение счётчика снова увеличит его».

Ответы для проверки: 1 это safety, 2 это idempotency, 3 показывает отсутствие гарантии idempotency у POST, 4 также показывает зависимость от смысла PATCH.

### Почему это важно для сети

Сеть может терять response, хотя сервер уже обработал request. Клиент видит таймаут и решает повторить вызов. Для чтения и полной замены последствия обычно предсказуемее. Для создания и операций вроде «увеличить на один» нужен дополнительный механизм защиты.

Это одна из причин, почему method является частью архитектурного договора. Он помогает заранее обсудить повторы, кэширование, восстановление и ошибки связи.

## 08. Как методы соединяются в одном Planner-сценарии

Теперь соберём не код сервера, а карту смыслов. У нас есть коллекция задач и отдельная Task. Для каждой операции сначала задаём вопрос «что хочет сделать клиент», затем выбираем method.

### Чтение коллекции

Клиент хочет показать список. Он не создаёт и не меняет Task. Значит, нужен GET.

### Чтение одной Task

Клиент хочет показать конкретную задачу. Это всё ещё чтение, даже если объект выбирается по id. Значит, нужен GET.

### Создание

Клиент сообщает данные новой задачи. Id ещё назначает сервер. Значит, нужен POST.

### Полная замена

Клиент передаёт полную форму существующей задачи. Значит, смысл выражает PUT.

### Частичное изменение

Клиент меняет только один или несколько разрешённых параметров и сохраняет остальные. Значит, смысл выражает PATCH.

### Удаление

Клиент просит убрать конкретную задачу из доступного состояния. Значит, нужен DELETE.

### Что именно будет в практике

В editor-практике мы не будем запускать сервер. Нужно будет написать одну маленькую функцию, которая получает название действия и возвращает соответствующий HTTP method.

Функция будет проверяться на шести смысловых сценариях:

- чтение коллекции;
- чтение одного объекта;
- создание;
- полная замена;
- частичное изменение;
- удаление.

Практика нужна не для механического запоминания английских слов. Она проверяет, умеете ли вы перевести предметное действие в сетевой contract. Особенно важно не спутать две операции обновления: полную замену и изменение части.

### Граница упражнения

В этом упражнении нет:

- словаря данных Planner;
- чтения и записи файла;
- HTTP-клиента;
- роутера;
- декораторов;
- обработки response.

Это намеренная граница. Сначала фиксируем семантику method. В следующих темах она будет соединена с ресурсом, адресом и полным договором API.

## 09. Самопроверка перед практикой

Ответьте на вопросы своими словами. Если ответ получается только как «так принято», вернитесь к причинной цепочке: намерение клиента, действие над состоянием, повтор и ожидаемый response.

1. Какую роль method выполняет внутри request?
2. Почему одного адреса недостаточно?
3. Что должен делать GET?
4. Почему GET не должен отмечать задачу выполненной?
5. Почему POST может создать дубликат при повторе?
6. Кто обычно назначает id новой Task?
7. Что означает полная замена в PUT?
8. Что происходит с отсутствующими полями при PUT?
9. Почему правило для отсутствующих полей нужно записать в contract?
10. Что передаёт PATCH?
11. Чем отсутствие поля отличается от null в PATCH?
12. Почему PATCH всё равно нуждается в валидации?
13. Что выражает DELETE?
14. Почему повторный DELETE может вернуть другой status?
15. Что такое safety?
16. Что такое idempotency?
17. Может ли idempotent request записать две строки в лог?
18. Почему POST обычно не считают idempotent?
19. Почему PATCH зависит от конкретной операции?
20. Почему method выбирают по смыслу, а не по имени Python-функции?
21. Какой метод нужен для чтения списка?
22. Какой метод нужен для создания?
23. Какой метод нужен для полной замены?
24. Какой метод нужен для частичного изменения?
25. Какой метод нужен для удаления?

### Короткий итог

Запомните не только таблицу, но и вопросы, которые нужно задавать перед реализацией:

~~~text
что хочет сделать клиент?
читает ли операция состояние?
создаёт ли она новую сущность?
заменяет ли форму целиком или меняет часть?
что произойдёт при повторе?
какой contract увидит клиент?
~~~

Если вы можете ответить на эти вопросы, editor-практика сводится к аккуратному переводу уже понятной модели в словарь соответствий.
