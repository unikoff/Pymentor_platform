# 116. Финальный проект 4: документация и защита

## Паспорт занятия

- **Блок:** 20. Остальные возможности FastAPI и Personal StudyHub.
- **Сквозной проект:** Personal StudyHub API.
- **Результат:** этап 5 завершён объяснимым Personal StudyHub.
- **Формат:** теория, чтение кода, предсказание, запуск, изменение, негативная проверка и объяснение результата.
- **Ориентировочное время:** 60–90 минут.
- **Стиль реализации:** синхронные endpoint-функции, обычный SQLAlchemy Session, без asyncio.

## Состояние проекта

**До занятия:** release candidate проходит тесты.

**Изменение:** документация, аудит и защита.

**После занятия:** этап 5 завершён объяснимым Personal StudyHub.

## Зачем это занятие

Доведём Personal StudyHub до передаваемого результата: проверим чистый запуск, напишем точный README, оформим OpenAPI и env-примеры, проведём security-аудит и защитим архитектурные решения на живом сценарии.

> **Связь с курсом.** Код и тесты уже проходят end-to-end. Последний урок не добавляет возможности, а доказывает воспроизводимость, безопасность и понимание всего пути запроса.

> **Граница темы.** Документация не маскирует незавершённый проект. README считается верным только после запуска по нему в чистой папке и совпадения описания с фактическим API.

## Маршрут занятия

1. **Очистить окружение.** клонировать проект или использовать новую папку, создать venv и заполнить .env по примеру.
2. **Воспроизвести систему.** alembic upgrade head → запуск → tests → ручной smoke scenario.
3. **Описать результат.** README, OpenAPI examples, error contract, security decisions и known limitations.
4. **Защитить проект.** проследить запрос, объяснить решение и внести маленькое изменение под наблюдением.

---

## 01. Готовый проект — это воспроизводимый проект

Фраза «у меня запускается» недостаточна. Другой разработчик должен получить репозиторий, выполнить конечный набор команд и увидеть ту же схему, API и тесты без устных подсказок.

### Code

реализует согласованный scope без placeholder и debug-обходов.

### Automation

migrations и tests повторяют состояние системы.

### Explanation

README и защита объясняют запуск, ограничения и ключевые решения.

### семь условий воспроизводимости

```python
reproducible_release = (
    clean_clone
    and documented_environment
    and alembic_upgrade_head
    and application_starts
    and tests_pass
    and manual_smoke_passes
    and no_secrets_in_git
)

print(reproducible_release)
```

### Что проследить в примере

- **run · Чистый запуск:** Команды README проверены на новой копии проекта. Пример: `venv -> env -> migrate -> uvicorn`.
- **test · Повторяемая проверка:** Негативные auth и ownership сценарии не требуют ручной настройки. Пример: `python -m pytest -q`.
- **explain · Осознанная защита:** Автор может проследить данные и назвать причину решения. Пример: `request -> dependency -> service -> DB`.

> **Контрольная граница:** Не добавляйте новую функцию в день защиты. Незакрытый scope лучше честно записать как limitation, чем скрыть нестабильной реализацией.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 02. README как инструкция с нулевого состояния

README пишется для человека без контекста. Он объясняет назначение, стек, требования, установку, конфигурацию, миграции, запуск, тесты, основные маршруты и известные ограничения.

### До запуска

версия Python, создание venv, установка зависимостей и .env.

### Запуск

migration command, uvicorn и адрес документации.

### Проверка

pytest, тестовая база и короткий ручной сценарий.

### минимальный каркас README

```markdown
# Personal StudyHub API

## Requirements
- Python 3.10+
- SQLite

## Local setup
python -m venv .venv
python -m pip install -r requirements.txt
cp .env.example .env
alembic upgrade head
uvicorn app.main:app --reload

## Tests
python -m pytest -q

## Documentation
http://127.0.0.1:8000/docs
```

### Что проследить в примере

- `Project goal` — что решает Personal StudyHub и для кого.
- `Setup` — команды от чистого клона до первого ответа.
- `Configuration` — таблица env-переменных без реальных секретов.
- `API flows` — registration, session, JWT, refresh и ownership.
- `Known limitations` — честные границы учебного релиза.

> **Контрольная граница:** README не должен содержать настоящий JWT secret, API key или рабочий пароль. .env.example показывает только имена и безопасные шаблонные значения.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 03. .env.example, секреты и репозиторий

Файл .env.example является контрактом конфигурации, а .env — локальным секретным состоянием. Перед релизом история Git проверяется на случайно добавленные credentials и файлы базы.

### .env.example

коммитится и перечисляет обязательные переменные.

### .env

игнорируется и содержит реальные development secrets.

### .gitignore

исключает venv, caches, .env, SQLite runtime files и upload directory.

### публичный шаблон и приватное состояние

```text
# .env.example
DATABASE_URL=sqlite:///./studyhub.db
JWT_SECRET=replace-with-random-secret
IMPORT_API_KEY=replace-with-random-key
COOKIE_SECURE=false

# .gitignore
.venv/
__pycache__/
.pytest_cache/
.env
*.db
uploads/
```

### Что проследить в примере

- **Проблемный код:** `JWT_SECRET=prod-secret-123`.
- **Вопрос:** Что нужно сделать до публикации?
- **Ответ:** убрать .env из коммита и заменить уже раскрытые секреты. Секрет, попавший в историю или отправленный наружу, считается раскрытым и требует rotation.

> **Контрольная граница:** Удаление строки из последнего коммита не отменяет утечку, если секрет уже был опубликован. Его нужно заменить у источника доверия.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 04. OpenAPI, примеры и ручной smoke flow

Swagger UI полезен только когда схемы, descriptions и status codes совпадают с реальностью. Финальная ручная проверка проходит один полный пользовательский путь и фиксирует ожидаемые ответы.

### Contract

response_model, documented errors и security schemes видны в OpenAPI.

### Examples

показывают безопасные request/response данные без секретов.

### Smoke flow

register → login → create task → read profile → upload → logout.

### ручной сценарий защиты

```python
smoke_flow = [
    "POST /auth/register -> 201",
    "POST /auth/token -> 200 access + refresh",
    "POST /tasks -> 201",
    "GET /tasks -> 200 only own tasks",
    "POST /tasks/{id}/attachments -> 201",
    "POST /auth/refresh -> 200 rotated pair",
    "reuse old refresh -> 401",
    "POST /auth/token/logout -> 204",
]

for step in smoke_flow:
    print(step)
```

### Что проследить в примере

- **Задача:** Расположите шаги так, чтобы аудитория увидела полный пользовательский путь.
  1. `показать чистую базу и alembic upgrade head`
  2. `зарегистрировать пользователя`
  3. `получить access/refresh или session cookie`
  4. `создать и прочитать свою задачу`
  5. `доказать отказ чужому пользователю или reuse token`
  6. `запустить pytest`
- **Почему:** Защита идёт от воспроизводимости к положительному и отрицательному поведению.

> **Контрольная граница:** Ручная демонстрация не заменяет tests. Она подтверждает, что автор умеет объяснить уже автоматизированный контракт.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 05. Security-аудит перед релизом

Финальный аудит проходит по поверхностям атаки, уже изученным в этапе 5. Для каждого пункта нужна ссылка на код или тест, а не ответ «должно работать».

### Credentials

password hashes, HttpOnly cookie, short access, refresh rotation и API key из env.

### Authorization

ownership, admin 403 и отсутствие доверия к user_id из body.

### Input

Pydantic validation, upload size/type/name и единые безопасные ошибки.

### контрольные security-инварианты

```python
security_checklist = {
    "passwords": "specialized hashing + no hash in responses",
    "sessions": "random id + hash at rest + expiry + revoke",
    "jwt": "signature + exp + type + short access",
    "refresh": "rotation + old-token rejection",
    "ownership": "task id + current user id",
    "admin": "explicit permission dependency",
    "upload": "size + allowlist + generated name",
    "secrets": "environment + no logs + no git",
}
```

### Что проследить в примере

- **Пароль:** Хешируется специализированной библиотекой и никогда не возвращается.
- **Cookie-session:** HttpOnly credential связан с отзывной записью на сервере.
- **Refresh token:** Rotation отзывает старую запись и выдаёт новую пару.
- **Ownership:** Поиск ресурса включает current_user.id.
- **Upload:** Сервер ограничивает размер и сам выбирает storage_name.
- **API key:** Хранится в environment и не попадает в URL или логи.

> **Контрольная граница:** Учебный аудит не заменяет профессиональный penetration test, но доказывает, что базовые риски не игнорируются.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 06. Как объяснить путь одного запроса

На защите важно не перечислять файлы, а проследить конкретный запрос. Хороший ответ показывает данные, проверки, транзакцию, response schema и возможные отказы.

### Вход

Authorization header и TaskPatch проходят parsing и Pydantic validation.

### Контекст

get_db и get_current_user выполняются до endpoint.

### Сценарий

service ищет owned task, меняет разрешённые поля, commit и возвращает ORM object.

### путь PATCH-запроса

```text
PATCH /tasks/42
Authorization: Bearer <access>
{"priority": 5}

client
  -> router + Pydantic
  -> get_db
  -> bearer dependency
  -> token decode
  -> load active user
  -> task service
  -> SELECT id=42 AND user_id=current_user.id
  -> update + commit + refresh
  -> TaskRead
  -> 200 JSON
```

### Что проследить в примере

1. Pydantic отделяет невалидное тело до бизнес-логики. Состояние: `payload=TaskPatch`.
2. Credential превращается в активного User. Состояние: `user=current user`.
3. Ownership входит в запрос к базе. Состояние: `filter=task_id + user.id`.
4. Меняются только разрешённые поля. Состояние: `task=dirty ORM object`.
5. Commit фиксирует транзакцию. Состояние: `transaction=committed`.
6. Response schema формирует безопасный JSON. Состояние: `status=200`.

> **Контрольная граница:** Если автор не может объяснить, где возникает 401, 404 или rollback, соответствующая часть проекта ещё не считается усвоенной.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 07. Живое изменение без разрушения проекта

Последняя часть защиты проверяет не память, а способность найти ответственное место. Наставник даёт маленькое изменение: новый filter, дополнительное разрешение или новый лимит upload.

### Найти контракт

определить route, schema, service и тест, которых касается изменение.

### Сделать минимально

не переписывать auth flow и соседние модули.

### Проверить регрессию

новый тест плюс полный существующий набор.

### пример задачи на защите

```text
change_request = "Admin can deactivate a user"

affected_surface = [
    "PATCH /admin/users/{user_id}/deactivate",
    "require_admin dependency",
    "user service deactivate operation",
    "UserRead response",
    "tests: admin success + user 403 + inactive login 401",
]
```

### Что проследить в примере

- **Вариант A — Править всё сразу:** Большой diff скрывает причину ошибки.
- **Вариант B — Один контракт:** Изменение ограничено конкретным сценарием.
- **Вывод:** Минимальный вертикальный срез сохраняет существующее поведение и облегчает ревью.

> **Контрольная граница:** Изменение считается завершённым после обновления README/OpenAPI, если внешний контракт действительно изменился.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## 08. Финальная контрольная точка этапа 5

Ученик завершает этап не списком терминов, а защищённым Personal StudyHub API. Проект запускается с нуля, проходит тесты и остаётся понятным без наставника рядом.

### Техническая готовность

migrations, auth flows, CRUD, rights, uploads и tests работают.

### Объяснение

различаются identity, authentication, authorization, session, JWT и API key.

### Передача

README, .env.example, OpenAPI и known limitations совпадают с кодом.

### готовность этапа 5

```python
final_gate = {
    "clean_setup": True,
    "migrations": True,
    "session_auth": True,
    "jwt_refresh_rotation": True,
    "ownership_and_admin": True,
    "safe_upload": True,
    "error_contract": True,
    "isolated_tests": True,
    "readme_verified": True,
    "defense_passed": True,
}

print(all(final_gate.values()))
```

### Что проследить в примере

- **Вопрос:** Какой главный результат этапа 5?
- **Ориентир ответа:** Воспроизводимый Personal StudyHub API с пользователями, session и JWT flow, refresh rotation, ownership, admin permission, безопасным upload, едиными ошибками, миграциями, изолированными тестами и объяснимой архитектурой.

> **Контрольная граница:** Следующий этап может вводить новые технологии только после сохранения этой базы качества. Завершённый монолит важнее набора незакреплённых инструментов.

### Мини-практика

1. Перепечатайте ключевой фрагмент без копирования.
2. Запустите положительный сценарий и зафиксируйте результат.
3. Измените одну входную границу или credential.
4. Получите ожидаемый отказ и объясните, на каком слое он возник.
5. Верните рабочую версию и добавьте короткий тест или запись в чек-лист.

---

## Итоговая проверка понимания

### Вопрос 1. Что доказывает чистый запуск?

1. проект воспроизводим без скрытого локального состояния
2. код содержит больше файлов
3. Swagger открыт в браузере

### Вопрос 2. Что хранится в .env.example?

1. имена переменных и безопасные шаблоны
2. реальные production secrets
3. пароли пользователей

### Вопрос 3. Что нужно уметь на защите?

1. проследить конкретный запрос и объяснить отказы
2. только перечислить библиотеки
3. показать число строк

### Вопрос 4. Когда изменение готово?

1. после focused test, full suite и обновления контракта
2. сразу после правки router
3. после нового названия ветки

## Ответы и объяснения

1. **проект воспроизводим без скрытого локального состояния** — Новая копия должна пройти setup, migrations, run и tests по документации.
2. **имена переменных и безопасные шаблоны** — Пример конфигурации коммитится без действительных credential.
3. **проследить конкретный запрос и объяснить отказы** — Понимание видно по пути данных и причинам решений.
4. **после focused test, full suite и обновления контракта** — Проверка регрессии и документация завершают изменение.

## Главное из занятия

- Завершённый проект воспроизводится с чистого клона.
- README описывает фактические setup, migrations, run и tests.
- .env.example коммитится, реальные secrets — нет.
- OpenAPI и ручной smoke flow должны совпадать с реализацией.
- Security-аудит ссылается на код и негативные тесты.
- Защита прослеживает один запрос через все слои.
- Живое изменение выполняется минимальным вертикальным срезом.
- Known limitations указываются честно.
- Этап 5 завершается Personal StudyHub API, а не набором фрагментов.

## Практическое задание

Проведите clean-room запуск, завершите README и .env.example, проверьте OpenAPI и security checklist, выполните smoke flow, запустите tests дважды и проведите защиту с объяснением PATCH /tasks/{task_id}.

### Обязательный порядок работы

1. Зафиксируйте текущее зелёное состояние тестов.
2. Реализуйте минимальный вертикальный срез.
3. Проверьте положительный сценарий.
4. Добавьте минимум два негативных сценария.
5. Запустите весь набор тестов повторно.
6. Обновите README или OpenAPI, если внешний контракт изменился.
7. Сделайте отдельный осмысленный Git-коммит.

## Критерии готовности

- Код запускается без ручных исправлений внутри базы данных.
- Используются только механизмы и зависимости, уже введённые в курсе.
- Ошибочные credentials или данные не запускают защищённую операцию.
- Тесты не используют development SQLite-файл.
- Секреты, password hash и внутренние пути не попадают в response.
- Ученик может проследить один запрос от клиента до commit/rollback и обратно.
- Ученик может объяснить, почему выбран конкретный status code.
- README и фактическое поведение не противоречат друг другу.

## Типичные ошибки

- Переносить business logic в router ради короткого service.
- Доверять `user_id`, filename, Content-Type или API key из небезопасного места.
- Возвращать клиенту traceback, hash или значение секрета.
- Использовать одну глобальную SQLAlchemy Session.
- Считать один успешный запрос достаточной проверкой security-сценария.
- Запускать тесты на той же SQLite-базе, что и локальное приложение.
- Добавлять новые технологии вместо завершения текущего контракта.

## Что должно остаться в проекте

После занятия Personal StudyHub должен демонстрировать результат: **этап 5 завершён объяснимым Personal StudyHub**.
