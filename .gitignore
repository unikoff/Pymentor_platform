.venv/
__pycache__/
*.py[cod]

backend/test.db
test.db
backend/*.log
backend/.env

frontend/node_modules/
frontend/*.log
# frontend/dist коммитим намеренно: сервер шаред-хостинга не может собрать
# фронт (не хватает памяти), поэтому готовый билд доставляем через git.

# Claude Code local config (личные разрешения + машинно-зависимые пути)
.claude/

# Локальный zip-архив билда (для ручной заливки; в git не нужен — dist уже в репе)
pymentor-dist.zip
