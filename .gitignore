.venv/
__pycache__/
*.py[cod]

backend/test.db
test.db
backend/*.log
backend/.env

frontend/node_modules/
frontend/*.log
# frontend/dist is intentionally tracked: the production host cannot build it.

# Claude Code local configuration.
.claude/

# Local ZIP archive of a build; frontend/dist is already tracked.
pymentor-dist.zip

# Local deployment notes with production details.
DEPLOYMENT.local.md
